#version 400 core

layout (location = 0) in vec2 position;
layout (location = 1) in vec2 texCoords;

out vec2 texCoords_;

uniform float height;
uniform float width;

void main(){
	
	gl_Position = vec4(position.x*width, position.y*height, 0.0 , 1.0);
	texCoords_ = texCoords;
	
}