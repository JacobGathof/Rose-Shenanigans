#version 400 core

in vec2 texCoords_;

uniform sampler2D image;

void main(){
	vec4 outColor = texture(image, texCoords_);
	if(outColor.a > .5 && outColor.a < .9){
		outColor.r = 0.0f;
		outColor.g = 0.0f;
		outColor.b = 1.0f;
		outColor.a = 1.0f;
	}
	gl_FragColor = outColor;
}