#version 400 core

in vec2 texCoords_;

uniform sampler2D world;
uniform sampler2D lights;

const vec4 ambient = vec4(.1,.1,.3,0);

void main(){

	vec4 outColor = (texture(lights, texCoords_)+ambient) * texture(world, texCoords_) * 4;

	gl_FragColor = outColor;

}


