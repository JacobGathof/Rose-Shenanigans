#version 400 core

in vec2 texCoords_;

uniform sampler2D image;

uniform vec3 color;

void main(){

	vec4 outColor = texture(image, texCoords_);
	
	gl_FragColor = vec4(outColor.xyz, outColor.r) * vec4(color, 1.0);

}

